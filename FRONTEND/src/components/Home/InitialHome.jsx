import Slider from "../Slider";

const InitialHome = () => {

  return (
    <div className='flex flex-col items-center mt-20 bg-slate-600 w-auto h-screen overflow-hidden'>
      <Slider />
    </div>
  );
};

export default InitialHome;
